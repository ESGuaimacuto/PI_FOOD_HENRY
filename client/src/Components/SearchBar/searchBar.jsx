


const SearchBar = () => {
  return (
    <div>
      <input id="Busqueda" type="text" placeholder="Buscar receta por nombre" />
      <button>Buscar</button>
    </div>
  );
};

export default SearchBar;
